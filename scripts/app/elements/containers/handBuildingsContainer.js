define(['pixi'], function (PIXI) {
    var container = new PIXI.Container();
    container.position.x = 1400;
    container.position.y = 540;
    container.name = "buildingsHand";

    return container;
});